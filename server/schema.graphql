type Query {
  jobs: [Job!]
}

type Company {
  id: ID!
  name: String!
  description: String
}

type Job {
  id: ID!
  title: String!
  description: String
  company: Company!
  """
  The date the job was posted, in ISO-8601 format (e.g. 2020-01-01).
  """
  date: String!
}
